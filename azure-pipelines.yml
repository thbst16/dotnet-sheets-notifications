name : dotnet-sheets-notifications
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: 'Build image'

  jobs:
  - job: Build
    displayName: 'Build and push docker image'
    pool:
      vmImage: $(vmImage)

    steps:
    - task: Docker@2
      displayName: 'Build the docker image'
      inputs:
        command: build